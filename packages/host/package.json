{
  "name": "@ff/host",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "comment": "The host runs on Preact (no React, no react->preact/compat alias). Plugins are React 19; they interoperate via the framework-agnostic remote-dom connection.",
  "dependencies": {
    "@ff/protocol": "*",
    "@preact/signals": "^1.3.2",
    "@preact/signals-core": "^1.8.0",
    "@quilted/threads": "^4.0.3",
    "@remote-dom/preact": "^1.3.0",
    "preact": "^10.29.2"
  },
  "devDependencies": {
    "vite": "^7.3.0"
  }
}
